import { ArtClassSearchPipe } from './art-class-search.pipe';

describe('ArtClassSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new ArtClassSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
