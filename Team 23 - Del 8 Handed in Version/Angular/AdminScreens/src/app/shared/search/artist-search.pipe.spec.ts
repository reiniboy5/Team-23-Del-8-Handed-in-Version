import { ArtistSearchPipe } from './artist-search.pipe';

describe('ArtistSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new ArtistSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
