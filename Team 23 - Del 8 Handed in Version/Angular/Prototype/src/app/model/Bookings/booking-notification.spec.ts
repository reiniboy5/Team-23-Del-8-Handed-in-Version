import { BookingNotification } from './booking-notification';

describe('BookingNotification', () => {
  it('should create an instance', () => {
    expect(new BookingNotification()).toBeTruthy();
  });
});
